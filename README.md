- we're using uv pip
- llm-reader uses selenium under the hood, may be important for efficiency reasons to note down since we're using playwright

### Setup instructions
1. `cd frontend`
2. `pnpm install` or `npm install`
3. `pnpm run dev` or `npm run dev` (if you used `npm` in the previous step)